name: Test Support CI

on:
  push:
    branches:
      - "main"
      - "phase/3.0-micro-services-architecture"
    paths:
      - 'test-support/**'
      - 'gradle/**'
      - '.github/workflows/test-support-ci.yml'
  pull_request:
    branches:
      - "main"
      - "phase/3.0-micro-services-architecture"
    paths:
      - 'test-support/**'
      - 'gradle/**'
      - '.github/workflows/test-support-ci.yml'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-test-support-${{ hashFiles('test-support/**/*.gradle.kts', 'gradle/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-test-support-

      - name: Grant execute permission for gradlew
        working-directory: test-support
        run: chmod +x gradlew

      - name: Build with Gradle
        working-directory: test-support
        run: ./gradlew build
