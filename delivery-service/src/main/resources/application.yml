server:
  port: 8083
  shutdown: graceful

spring:
  application:
    name: delivery-service
  lifecycle:
    timeout-per-shutdown-phase: 30s
  docker:
    compose:
      file: delivery-service/docker-compose.yml
  profiles:
    include:
      - db
      - kafka
      - monitoring
