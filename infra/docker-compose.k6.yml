services:
  k6:
    image: grafana/k6:latest
    container_name: k6
    volumes:
      - ./k6:/scripts
    working_dir: /scripts
    entrypoint: ["k6"]
    environment:
      - BASE_URL=http://host.docker.internal:8080
    command: ["run", "order-test.js"]
