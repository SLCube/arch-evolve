server:
  port: 8082

spring:
  application:
    name: payment-service
  docker:
    compose:
      file: payment-service/docker-compose.yml
  profiles:
    include:
      - db
      - kafka
      - monitoring
