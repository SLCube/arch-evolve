server:
  port: 8082

pg:
  base-url: http://localhost:9091

spring:
  application:
    name: payment-service
  docker:
    compose:
      file: payment-service/docker-compose.yml
  profiles:
    include:
      - db
      - kafka
      - monitoring
